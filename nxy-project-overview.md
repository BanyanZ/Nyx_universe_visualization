# 项目概览 — Nyx 宇宙学模拟可视化系统

> 赛题 II: 科学可视化挑战赛 · 基于 Nyx 宇宙学模拟密度场数据

## 一、项目定位

本项目针对 Nyx 宇宙学模拟的 100 个时间步密度场数据 (128^3 float32)，构建了一套浏览器端交互式可视化分析系统。系统从数据加载、统计分析、体渲染到交互联动，形成完整的可视化管线。

## 二、赛题任务完成情况

### 任务一：体数据渲染

**实现方案：双渲染管线**

1. **标准体渲染 (Volume Rendering)**
   - 采用 VTK `vtkSmartVolumeMapper` GPU Ray Casting
   - 纯自发光模式 (Emission Only, `ShadeOff()`)，更贴合宇宙自发光气体的物理特性
   - 数据经 Gamma=4 幂律变换增强对比度后映射至 [0,1]
   - 物理驱动传递函数将标量值映射至宇宙结构类型：
     - 空洞 (Voids): 黑色，完全透明
     - 薄壁 (Walls): 深蓝 `#0f3460`，微弱不透明度
     - 纤维 (Filaments): 紫色 `#533483`，中等不透明度
     - 节点 (Nodes): 红粉 `#e94560` -> 暖桃 `#fcd5ce`，高不透明度
     - 密度峰值: 近白色，最高不透明度
   - 提供 5 种预设 (Cosmic Web / Top 1% / Voids / Filaments / Show All)

2. **沉浸式粒子星空 (Immersive Starfield)**
   - 独立渲染管线，开启后关闭体渲染，切换至多层粒子系统
   - 密度加权重要性采样 (density^1.75)，从密度场中抽取最多 120,000 个粒子
   - 6 层渲染叠加：
     - **Field Stars** — 各向同性平铺场星，覆盖整个视野
     - **Background Stars** — 密度较低区域的微弱衬底星
     - **Main Stars** — 主星层，密度越高越亮越大
     - **Mist** — 蓝紫色密度雾，轻柔大气层效果
     - **Nebula Glow** — 高密度区的发光星云
     - **Veil** — 最密区域的大尺度漫射光晕
   - 相机感知视锥剔除：根据当前相机方向和位置裁剪粒子
   - 自适应 LOD：zoom ratio 从 0.38x 到 2.3x 动态调节粒子数量和尺寸
   - 异步监控循环每 220ms 检测相机变化并更新粒子

### 任务二：演化规律归纳

- 预计算全部 100 个时间步的统计指标 (均值、标准差、偏度、峰度、分位数)
- 通过 Stats Evolution 弹窗展示四面板演化曲线 (Mean / Std Dev / Skewness / Kurtosis)
- 当前时间步以红色虚线标注
- 点击任意数据点自动跳转至对应时间步，全视图同步更新

### 任务三：时序统计分析

- **密度对数直方图** — 200 bin，固定范围 [7.5, 14.5]，支持框选密度区间
- **脊线图 (Ridge Plot)** — 每 5 个时间步一条脊线 (共 20 条)，蓝 -> 红渐变表示早期 -> 晚期
- **统计量演化曲线** — 四指标演化趋势，点击跳转联动
- **高密度比例追踪** — >P99 体素比例随时间的变化

### 任务四：交互仪表板

**双向联动机制：**

| 操作 | 效果 |
|------|------|
| 直方图框选 | 3D 视图高亮对应密度范围体素 (点云叠加 + 传递函数切换) |
| 投影图框选 | 3D 视图显示 ROI wireframe 包围盒，统计面板切换为子集数据 |
| Ridge 点击 | 跳转至对应时间步 |
| Stats 点击 | 跳转至对应时间步 |
| Focus 3D | 自动计算并聚焦选中区域的相机位姿 |
| Clear All | 一键清除所有联动状态 |

**联动状态面板：**
- Density Brush 面板：显示选中密度范围
- Spatial ROI 面板：显示选中空间区域和体素数
- Linked View 面板：汇总选择统计 (Scope / Voxels / Matched / Sel Mean / Sel P99 / 3D Sample)

## 三、界面设计

### 视觉风格

- **Glassmorphism** — 所有控制面板采用磨砂玻璃效果 (`backdrop-filter: blur(18px)` + 半透明 `rgba(22,33,62,0.82)` 背景)
- **统一色板** — 六色暗色系主题，从数据渲染到 UI 组件全局统一：

| 名称 | 色值 | 用途 |
|------|------|------|
| bg | `#1a1a2e` | 页面背景 |
| panel | `#16213e` | 面板/卡片背景 |
| mid | `#0f3460` | 边框/滑块轨道/直方图 bar |
| purple | `#533483` | 辅助按钮/分析入口 |
| red | `#e94560` | 强调色/标题下划线/高亮数据 |
| text | `#fcd5ce` | 文字/节点渲染色 |

- **自定义 Vuetify 3 主题** — 通过 `vuetify_config` 参数注入完整暗色主题，确保所有组件一致
- **无边框沉浸布局** — 隐藏 Trame 默认 footer 和导航图标，toolbar 也做半透明毛玻璃处理

### 响应式浮动面板

- 左上 (190px): 预设 + 渲染选项 + 分析入口
- 左下 (190px): 实时统计面板
- 右下 (440x260px): 密度直方图
- 条件面板: Spatial ROI / Density Brush / Linked View (联动激活时出现)

## 四、技术实现

### 核心技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 数据层 | NumPy | float32 二进制读取，Fortran order reshape，Gamma 对比度增强 |
| 统计层 | SciPy + NumPy | skewness/kurtosis 计算，直方图/分位数，全时间步预计算 |
| 渲染层 | VTK + PyVista | GPU Ray Casting 体渲染 + Glyph 粒子系统 |
| 后处理 | SciPy (gaussian_filter) | Sigma Clip -> Bloom -> Reinhard Tone Mapping -> Gamma |
| 交互层 | Trame + Vuetify 3 + Plotly | Web 服务 + 组件库 + 图表交互 |
| 传输层 | VtkRemoteView | 服务端渲染结果通过 WebSocket 流式推送至浏览器 |

### 关键算法

**密度加权重要性采样 (Immersive 模式)：**
- 以第 63 百分位为阈值筛选候选体素
- 权重 = (density - floor)^1.75
- 带亚体素抖动 (sub-voxel jitter) 消除网格感
- 焦点计算：以 P99.65 以上体素的密度加权质心为锚点

**相机感知视锥剔除：**
- 从 VTK 相机提取 right/up/forward 正交基
- 将粒子投影到视图空间，按视锥半宽/半高/前后深度裁剪
- 可见粒子不足 5000 时自动放宽 1.4x

**多层粒子颜色系统：**
- 星光色: 深蓝 -> 紫蓝 -> 冰蓝 -> 白 -> 金 (密度/深度/中心距加权混合)
- 雾气色: 墨蓝 -> 靛蓝 -> 蓝 -> 薰衣草 (三角函数空间调制)
- 星云色: 深蓝 -> 电蓝 -> 青 -> 紫 -> 白 (密度^1.25 热度映射)
- 场星色: 深蓝 -> 冰蓝 -> 白 -> 金 (各向同性周期调制)

**Brush 选择高亮：**
- 在选中密度范围内重要性采样最多 3200 个体素 (播放时 1400)
- 采样点生成 Glyph 点云，颜色从冰蓝 -> 暖黄 -> 热红过渡
- 同时更新体渲染传递函数，仅高亮选中密度区间

### 性能优化

- 播放模式下跳过直方图重建，避免清除 Brush 状态
- 沉浸模式粒子采样缓存 (LRU, 最多 8 个时间步)
- 相机签名去重，仅在视角实际改变时重建粒子层
- Brush 选中时使用确定性随机种子 (基于时间步 + ROI + 密度范围)，避免闪烁

## 五、文件说明

| 文件 | 行数 | 职责 |
|------|------|------|
| `app.py` | ~1790 | 主入口：UI 布局、交互回调、沉浸模式粒子管线、联动逻辑 |
| `batch_export.py` | ~215 | 批量渲染脚本：关键帧、动画、统计图表导出 |
| `nyx/data.py` | ~41 | 数据加载：二进制读取、Gamma 增强、VTK 转换 |
| `nyx/stats.py` | ~185 | 统计分析：直方图、脊线图、统计量演化曲线 |
| `nyx/transfer_functions.py` | ~125 | 传递函数：颜色映射、不透明度映射、5 种预设、Brush 高亮 |
| `nyx/volume.py` | ~63 | 体渲染管线：VTK Smart Volume Mapper 配置 |
| `nyx/visualizations.py` | ~216 | 扩展可视化：投影图、等值面叠加、多面板拼图 |
| `nyx/postprocess.py` | ~74 | 图像后处理：Sigma Clip、Bloom、Reinhard Tone Mapping |

## 六、运行指南

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 放置数据集
# 确保 1-IINyx_dataset/Nyx/0000.dat ~ 0099.dat 存在

# 3. 启动仪表板
python app.py
# 浏览器打开 http://localhost:8080

# 4. (可选) 批量导出
python batch_export.py --all
```
